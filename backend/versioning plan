from fastapi import FastAPI, APIRouter

app = FastAPI(
    title="VISTRA API",
    version="1.0.0",
)

v1 = APIRouter(prefix="/api/v1")

@v1.get("/users")
async def get_users():
    ...

app.include_router(v1)